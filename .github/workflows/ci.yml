name: Bash CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  check-bash:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: Install spell check
        run: sudo apt-get install -y spellcheck
      - name: linting
        run: spellcheck analyse.sh
      - name: make executable
        run: chmod +x analyse.sh
      - name: run the scrpit
        run: ./analyse.sh
